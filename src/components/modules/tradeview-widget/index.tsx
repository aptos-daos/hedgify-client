"use client";

import React from "react";
import { DaoData } from "@/validation/dao.validation";

const TradeViewWidget: React.FC<DaoData> = ({}) => {
  return (
    <div>
    </div>
  );
};

export default TradeViewWidget;
